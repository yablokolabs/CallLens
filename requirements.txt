# Runtime dependencies for the MCP server on MCPize (python runtime).
# The server adds packages/calllens/src to sys.path itself, so this only
# lists third-party packages — no editable installs or extras.
fastmcp>=2.0
pydantic>=2.7
pydantic-settings>=2.3
pyyaml>=6.0
httpx>=0.27
langchain>=1.0
langchain-openai>=1.0
langchain-anthropic>=1.0
langgraph>=1.0
