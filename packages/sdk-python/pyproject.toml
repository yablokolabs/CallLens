[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "calllens-sdk"
version = "0.1.0"
description = "Python SDK for CallLens conversation intelligence."
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = ["calllens>=0.1.0", "httpx>=0.27"]

[tool.setuptools]
packages = []
